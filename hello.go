package main
/*
import (
	"LEARN_GO/types"
	"fmt"
)
import "rsc.io/quote"

/*func main() {
	fmt.Println(quote.Go())

	//types.Condistional(-1)

	//types.Base()

	//_, name := types.Name()
	//fmt.Println("my name is ", name)

	//fmt.Println(types.NAKED_Return())
	//x, _, name := types.NAKED_Return()w
	//fmt.Println(x, name)
	/*address1 := types.Address{City: "udine", Country: "italy", Street: "poscolle"}
	user1 := types.User{FirstName: "ameni", LastName: "guesmi", Address: address1}
	fmt.Println(types.Creat_user(user1))*/

	/*fmt.Println((types.Create_struct()).Model) // acess directly without the need of typing structName(truck).embedded_structName(car).filed(model)
	fmt.Println(types.Create_struct())*/

	/*rec := types.Rectangle{
		Width: 5, Lenght: 9,
	}
	fmt.Println(rec.Area())
	address1 := types.Address{City: "udine", Country: "italy", Street: "poscolle"}
	user1 := types.User{FirstName: "ameni", LastName: "guesmi", Address: address1}
	fmt.Println(user1.Creat_user())

	auth := types.Authinfo{
		Password: "123",
		Username: "admin",
	}
	fmt.Println(auth.Autorization())
}*/
